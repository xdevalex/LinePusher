services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: fastify-ts-app:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
